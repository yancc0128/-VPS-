# VPS 半自动化部署助手

一个面向 macOS与Windows 的 VPS 半自动化部署工具，专注于简化远程服务器初始化、代理环境配置以及常用服务部署流程。

## v0.2.0

### 新增功能
- **SSH 私钥登录**：密码之外新增私钥认证（带可选 passphrase），sudo 密码与登录凭据解耦（私钥 + NOPASSWD 可留空）；CLI 支持 `--private-key`（路径或内联 PEM）/ `--key-passphrase` 及对应环境变量。
- **单连接复用 + 流式输出 + 可中断**：部署向导从“每步重连”改为一条 SSH 会话贯穿全部 9 步，远端输出实时流式显示，可随时取消当前步骤。
- **执行前命令预览确认**（默认开启）：开始部署前先弹出整套远端脚本预览，确认后才连接服务器，兑现“命令执行前必须确认”的隐私原则。

### 体验
- 界面整体重做为深色技术控制台主题：分层表面与景深、语义化配色（绿=安全、琥珀=警告、红=危险）、进度轨与终端式实时输出。

### 修复与维护
- 修复 SSH 数据流缺少 `error` 监听可能崩溃主进程；新增远程命令执行超时看门狗，避免命令卡死导致界面永久卡住。
- 安全报告 AI 状态改为按实际配置显示；移除未实现的 hysteria2/tuic 死数据，统一 vless-only。
- 新增 `scripts/ssh-auth.cjs` 单测，测试增至 23 项；新增 WORKFLOW.md 部署流程文档。

## v0.1.0

### 安全加固
- 新增 SSH 主机密钥校验（TOFU）：首次连接记录指纹并持久化，之后比对，不一致时拒绝连接，可防中间人攻击；CLI 支持 `--host-fingerprint` 预校验。
- 远程部署脚本防注入：xray 配置改为 base64 传输 + 远程解码写入，协议名走白名单，消除命令注入与 heredoc 提前结束风险。
- 收紧应用安全策略：新增严格 CSP（渲染层 `connect-src 'none'`）、限制页面跳转、防止 SSH 调用重复回调。

### 新增功能
- 新增**可选、默认关闭、可随时跳过**的 AI 诊断解读：把自愈/诊断输出转成自然语言结论与排错建议。
  - 发送前脱敏（IP / 域名 / 密钥 / 订阅链接等），绝不发送密码与主机指纹。
  - API Key 仅保存在本机（`0600`），不回传界面、不写日志。
  - AI 只解读、不执行任何远程命令；不可用时自动回退本地规则总结。
  - 默认对接 DeepSeek（OpenAI 兼容接口），接口地址与模型可自定义。

## 项目功能

- 一键初始化 VPS 基础环境
- 自动安装常用运行依赖
- 快速配置代理与网络环境
- 支持远程 SSH 部署流程
- 简化服务器首次配置步骤
- 减少手动命令操作与重复配置
- 提供更统一的部署体验

## 核心特点

- 面向个人开发者与轻量运维场景
- 原生适配 macOS（Apple Silicon）
- 轻量化设计
- 降低 VPS 部署门槛
- 提高远程环境搭建效率

## 适用场景

- 新 VPS 快速初始化
- 代理环境部署
- 长期运行服务部署
- 个人远程基础设施管理

## 后续规划

- 自动更新机制
- Docker 集成
- 多系统支持
- 可视化部署界面
- 服务状态检测
- 一键环境备份与恢复

## 项目状态

当前版本处于早期开发阶段，后续功能与架构仍会持续完善与迭代。
