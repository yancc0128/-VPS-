# VPS 半自动化部署助手

一个面向 macOS与Windows 的 VPS 半自动化部署工具，专注于简化远程服务器初始化、代理环境配置以及常用服务部署流程。

## 本次更新

### 安全加固
- 新增 SSH 主机密钥校验（TOFU）：首次连接记录指纹并持久化，之后比对，不一致时拒绝连接，可防中间人攻击；CLI 支持 `--host-fingerprint` 预校验。
- 远程部署脚本防注入：xray 配置改为 base64 传输 + 远程解码写入，协议名走白名单，消除命令注入与 heredoc 提前结束风险。
- 收紧应用安全策略：新增严格 CSP（渲染层 `connect-src 'none'`）、限制页面跳转、防止 SSH 调用重复回调。

### 新增功能
- 新增**可选、默认关闭、可随时跳过**的 AI 诊断解读：把自愈/诊断输出转成自然语言结论与排错建议。
  - 发送前脱敏（IP / 域名 / 密钥 / 订阅链接等），绝不发送密码与主机指纹。
  - API Key 仅保存在本机（`0600`），不回传界面、不写日志。
  - AI 只解读、不执行任何远程命令；不可用时自动回退本地规则总结。
  - 默认对接 DeepSeek（OpenAI 兼容接口），接口地址与模型可自定义。

## 项目功能

- 一键初始化 VPS 基础环境
- 自动安装常用运行依赖
- 快速配置代理与网络环境
- 支持远程 SSH 部署流程
- 简化服务器首次配置步骤
- 减少手动命令操作与重复配置
- 提供更统一的部署体验

## 核心特点

- 面向个人开发者与轻量运维场景
- 原生适配 macOS（Apple Silicon）
- 轻量化设计
- 降低 VPS 部署门槛
- 提高远程环境搭建效率

## 适用场景

- 新 VPS 快速初始化
- 代理环境部署
- 长期运行服务部署
- 个人远程基础设施管理

## 后续规划

- 自动更新机制
- Docker 集成
- 多系统支持
- 可视化部署界面
- 服务状态检测
- 一键环境备份与恢复

## 项目状态

当前版本处于早期开发阶段，后续功能与架构仍会持续完善与迭代。
